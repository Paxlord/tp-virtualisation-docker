version: "3.9"
services: 
    nodeapp:
        build: ./node-app
        ports: 
            - "4300:8080"
        restart: always
    reactapp:
        build: ./react-app
        ports:
            - "4301:3000"
        restart: always
    
